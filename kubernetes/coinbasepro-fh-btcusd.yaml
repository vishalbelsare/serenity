apiVersion: apps/v1
kind: Deployment
metadata:
  name: coinbasepro-fh
  labels:
    app: coinbasepro-fh
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coinbasepro-fh
  template:
    metadata:
      labels:
        app: coinbasepro-fh
    spec:
      containers:
        - name: phemex-fh
          image: cloudwallcapital/serenity:2020.10.22-b45
          command: ["/app/venv-py3/bin/python"]
          args: ["serenity/marketdata/fh/coinbasepro_fh.py", "--include_symbol=BTC-USD"]
          env:
            - name: POSTGRES_SERENITY_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: database-secret-config
                  key: serenity-password
          volumeMounts:
            - mountPath: /behemoth
              name: behemoth 
      volumes:
        - name: behemoth
          persistentVolumeClaim:
            claimName: behemoth-pv-claim
